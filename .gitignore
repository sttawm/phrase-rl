# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
uv.lock

# Data / models — checkpoints are synced explicitly, never committed wholesale
results/checkpoints/*
!results/checkpoints/.gitkeep
!results/checkpoints/metrics.json
# curated checkpoint archive: 90MB split tars + manifests (scripts/ckpt_archive.sh)
!results/checkpoints/archive/
!results/checkpoints/archive/**
data/
*.safetensors
*.pt
*.ckpt

# Secrets
.env
*.key

# OS / editor
.DS_Store
.idea/
.vscode/
