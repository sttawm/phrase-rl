# Session auto-memory (snapshot)

Copied from `~/.claude/projects/-Users-sttawm-dev-robotics-phrase-rl/memory/` on
2026-09-04 because that directory exists only on the laptop that wrote it and did
NOT survive the move to a replacement Mac. These are operational lessons — pod
bringup footguns, shell pitfalls, checkpoint discipline — not experiment results.

`MEMORY.md` is the index; each other file is one lesson. They are point-in-time
observations: verify a file/flag still exists before acting on it.

To restore into a live session's memory on another machine, copy the `.md` files
back into that machine's
`~/.claude/projects/<project-slug>/memory/` directory.
